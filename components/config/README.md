# Config Component

Reserved for platform configuration and build/runtime settings.

Phase 1 keeps configuration behavior unchanged. Later phases will move board- and network-specific settings behind a cleaner configuration boundary.
